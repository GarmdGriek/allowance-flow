[build]
builder = "nixpacks"
buildCommand = "pip install uv && uv sync --group base --group app"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
