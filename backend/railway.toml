[build]
builder = "nixpacks"
buildCommand = "pip install uv && uv export --group base --group app --no-hashes -o /tmp/full-requirements.txt && pip install -r /tmp/full-requirements.txt"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
